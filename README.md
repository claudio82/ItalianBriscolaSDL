# ItalianBriscolaSDL
Italian Briscola card game written in C++ using SDL 3.

CMake, SDL3, SDL_Image (v3), SDL_TTF (v3) required.

Run these commands to build:

cmake -S . -B build

cmake --build build

Then copy the assets folder inside your build folder.

